# always start MacVim in terminal instead of in separate window
alias 'mvim=mvim -v' 
# enable colourized output
alias 'ls=ls -G'
# for Mac OS X users that are spending too much time on Windows ;-)
alias 'start=open'
 

